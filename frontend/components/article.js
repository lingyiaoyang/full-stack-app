import React from 'react';
import { text } from '../assets/text/text.json';

const article = () => {
  return (
    <div id='Article' style={{ marginTop: '50px' }}>
      {text}
      {text}
      {text}
      {text}
      {text}
    </div>
  );
};

export default article;
